apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  labels:
    app: webapp
spec:
  # TODO: Define the service type (use NodePort for external access)
  # type: 
  selector:
    # TODO: Define selector to match deployment pods (must match deployment labels)
    # app: 
  ports:
    # TODO: Complete the port configuration
    # - protocol:    # Protocol type (TCP for HTTP traffic)
    #   port:        # Port exposed by the service (try 80)
    #   targetPort:  # Port on the container (nginx uses 80)
    #   nodePort:    # External port on node (use 30080 or similar)
  type: NodePort