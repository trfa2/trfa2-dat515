services:
  app:
    build: .
    ports:
      - "5001:5000" # Direct access for debugging (using 5001 to avoid port conflicts)
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1

  database:
    ports:
      # TODO: Expose database on port 3306

  cache:
    ports:
      # TODO: Expose Redis for debugging on port 6379
